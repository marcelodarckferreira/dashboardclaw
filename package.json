{
  "name": "dashboardclaw",
  "version": "1.0.0",
  "description": "DashboardClaw for OpenClaw that enhances the gateway UI with auto-reconnect and connection monitoring",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && cp src/inject.js dist/inject.js",
    "deploy": "npm run build && cp -r dist/* ../../../extensions/dashboardclaw/dist/ && cp src/inject.js ../../../extensions/dashboardclaw/dist/inject.js && echo 'Deployed to extensions. Restart gateway to apply.'",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepublishOnly": "npm run build",
    "release": "node -e \"const v=process.argv[1]; if(!v){console.error('Usage: npm run release -- X.Y.Z');process.exit(1)} const fs=require('fs'); const pkg=JSON.parse(fs.readFileSync('package.json','utf8')); pkg.version=v; fs.writeFileSync('package.json',JSON.stringify(pkg,null,2)+'\\n'); const plug=JSON.parse(fs.readFileSync('openclaw.plugin.json','utf8')); plug.version=v; fs.writeFileSync('openclaw.plugin.json',JSON.stringify(plug,null,2)+'\\n'); console.log('Updated package.json and openclaw.plugin.json to v'+v);\" --"
  },
  "keywords": [
    "openclaw",
    "dashboardclaw",
    "gateway",
    "websocket",
    "auto-reconnect"
  ],
  "author": "Marcelo Darck",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/marcelodarckferreira/dashboardclaw.git"
  },
  "bugs": {
    "url": "https://github.com/marcelodarckferreira/dashboardclaw/issues"
  },
  "homepage": "https://github.com/marcelodarckferreira/dashboardclaw#readme",
  "files": [
    "dist",
    "!dist/**/*.map",
    "openclaw.plugin.json",
    "README.md"
  ],
  "optionalDependencies": {
    "node-pty": "^1.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "jsdom": "^26.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  },
  "openclaw": {
    "extensions": [
      "dist/index.js"
    ]
  }
}
